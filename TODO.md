# HybridSocial TODO

## Migration
- [ ] Pleroma migration tool — import users, posts, follows, blocks, RSA keypairs, actor URLs
- [ ] Mastodon migration tool — same as above but from Mastodon's data format

## Deferred Features
- [ ] Status expiry / Stories (auto-deleting posts after configurable TTL)
